<UserControl x:Class="PowerTools.Views.UserControls.ReleaseInformation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:behaviours="clr-namespace:PowerTools.Core.Behaviours;assembly=PowerTools.Core"
             xmlns:md="clr-namespace:MdXaml;assembly=MdXaml"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/PowerTools.Core;component/Themes/Generic.xaml"/>
                <ResourceDictionary Source="../../Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <TabControl Style="{StaticResource TabControl.Normal}" Background="{StaticResource Background.White}" behaviours:TabControlBehavior.CanCloseTabItem="False">
            <TabItem Header="Details">
                <md:MarkdownScrollViewer Markdown="{Binding Path=Details, UpdateSourceTrigger=PropertyChanged}" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"/>
            </TabItem>
            <TabItem Header="Changelogs">
                <md:MarkdownScrollViewer Markdown="{Binding Path=ChangeLogs, UpdateSourceTrigger=PropertyChanged}" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"/>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
