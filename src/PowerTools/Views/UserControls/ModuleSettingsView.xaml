<UserControl x:Class="PowerTools.Views.UserControls.ModuleSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="750"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <prism:Dialog.WindowStyle>
        <Style TargetType="Window">
            <Setter Property="prism:Dialog.WindowStartupLocation" Value="CenterOwner" />
            <Setter Property="MinHeight" Value="300"/>
            <Setter Property="MaxHeight" Value="300"/>
            <Setter Property="MinWidth" Value="750"/>
            <Setter Property="MaxWidth" Value="750"/>
            <Setter Property="WindowStyle" Value="ToolWindow"/>
        </Style>
    </prism:Dialog.WindowStyle>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DataGrid Style="{StaticResource DataGrid.Normal}" BorderBrush="Transparent" BorderThickness="0" Margin="0 5 0 0" ItemsSource="{Binding ModuleSettings, UpdateSourceTrigger=PropertyChanged}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Key" Binding="{Binding Key}" IsReadOnly="True" MinWidth="200" />
                <DataGridTextColumn Header="Value" Binding="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" MinWidth="350"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" >
            <Button Style="{StaticResource Button.Icon}" Command="{Binding CmdRevertSettings}" Content="&#xE149;" Tag="Revert" MinWidth="100" Margin="5"/>
            <Button Style="{StaticResource Button.IconActive}"  Command="{Binding CmdSaveSettings}" Content="&#xE105;" Tag="Save" MinWidth="100" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>
