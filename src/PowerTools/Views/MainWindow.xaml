<Window x:Class="PowerTools.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:configurations="clr-namespace:PowerTools.Core.Configurations;assembly=PowerTools.Core"
        xmlns:userControls="clr-namespace:PowerTools.Views.UserControls"
        xmlns:controls="clr-namespace:PowerTools.Core.Controls;assembly=PowerTools.Core"
        xmlns:behaviours="clr-namespace:PowerTools.Core.Behaviours;assembly=PowerTools.Core"
        prism:ViewModelLocator.AutoWireViewModel="True"
        xmlns:data="clr-namespace:PowerTools.Views"
        mc:Ignorable="d"
        d:DesignHeight="450" d:DesignWidth="800"
        Background="{StaticResource Background.WhiteSmoke}"
        Title="PowerTools"
        WindowStartupLocation="CenterScreen" 
        MinWidth="{Binding Source={x:Static configurations:ModuleGlobalSettings.Instance}, Path=WindowSettings.MinWidth}"
        MaxWidth="{Binding Source={x:Static configurations:ModuleGlobalSettings.Instance}, Path=WindowSettings.MaxWidth}"
        Width="{Binding Source={x:Static configurations:ModuleGlobalSettings.Instance}, Path=WindowSettings.Width}"
        MinHeight="{Binding Source={x:Static configurations:ModuleGlobalSettings.Instance}, Path=WindowSettings.MinHeight}"
        MaxHeight="{Binding Source={x:Static configurations:ModuleGlobalSettings.Instance}, Path=WindowSettings.MaxHeight}"
        Height="{Binding Source={x:Static configurations:ModuleGlobalSettings.Instance}, Path=WindowSettings.Height}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="4"/>
            <RowDefinition Height="{Binding ViewLogGridLength, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" MaxHeight="500"/>
            <RowDefinition Height="22"/>
        </Grid.RowDefinitions>
        <userControls:SideBar Grid.Column="0" Grid.Row="0" Grid.RowSpan="3"/>
        <ContentControl Grid.Column="1" Grid.Row="0" prism:RegionManager.RegionName="{x:Static configurations:Constants.ModuleRegionName}"
                        Focusable="False"
                        IsEnabled="{Binding IsFree, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
        <userControls:WindowLoading Grid.Column="1" Grid.Row="0"/>
        <GridSplitter Style="{StaticResource GridSplitter.Normal}" Grid.Column="1" Grid.Row="1" HorizontalAlignment="Stretch" Background="Transparent"/>
        <userControls:LogsView Grid.Column="1" Grid.Row="2"/>
        <userControls:StatusBar Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="3" />
        <!--<Grid Grid.ColumnSpan="2" Grid.RowSpan="4" Margin="0" Background="White">
            <controls:PaginationDataGrid Header="ABC" 
                                         Filters="{x:Static data:MainWindow.ColumnFilters}"
                                         ColumnSettings="{x:Static data:MainWindow.ColumnSettings}">
                <controls:PaginationDataGrid.HeaderSettings>
                    <StackPanel>
                        <GroupBox Header="Message Displays" Margin="0">
                            <StackPanel>
                                <CheckBox Style="{StaticResource CheckBox.Normal}" IsChecked="{Binding MessageFilters.DoShowUnreadOnly, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Content="Show Unreads" Margin="5 10 5 0"/>
                                <TextBlock Style="{StaticResource TextBlock.ContentSmall}" Text="Filters on message source and displays unread messages only" TextWrapping="Wrap" Foreground="DimGray"/>
                            </StackPanel>
                        </GroupBox>
                        <GroupBox Header="Sorting" Margin="0 5 0 0">
                            <StackPanel>
                                <CheckBox Style="{StaticResource CheckBox.Normal}" IsChecked="{Binding MessageFilters.UseDefaultSorts, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Content="Use Default Sorting" Margin="5 10 5 0"/>
                                <TextBlock Style="{StaticResource TextBlock.ContentSmall}" Text="Be careful. Sorting during streaming data can reduce performance." TextWrapping="Wrap" Foreground="DimGray"/>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </controls:PaginationDataGrid.HeaderSettings>
                <controls:PaginationDataGrid.Columns>
                    <controls:PaginationDataGridTextColumn Name="UTCTimestamp" Header="Timestamp (UTC)"  IsReadOnly="True" MinWidth="100"/>
                    <controls:PaginationDataGridTextColumn Name="CustomerId" Header="CustomerId"  IsReadOnly="True" MinWidth="100"/>
                    <controls:PaginationDataGridTextColumn Name="DocumentId" Header="DocumentId"  IsReadOnly="True" MinWidth="100"/>
                </controls:PaginationDataGrid.Columns>
            </controls:PaginationDataGrid>
            <TabControl Style="{StaticResource TabControl.Normal}">
                <TabItem Style="{StaticResource TabItem.Normal}" Header="ABC"/>
            </TabControl>
        </Grid>-->
    </Grid>
</Window>
